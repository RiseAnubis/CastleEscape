<Window x:Class="CastleEdit.MessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CastleEdit"
        mc:Ignorable="d"
        Title="MessageBoxWindow" Height="200" Width="350" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True" WindowStartupLocation="CenterOwner">
    <Grid Name="mainGrid" Background="{StaticResource BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Grid.Column="0" Width="40" Height="40" Name="imgIcon"/>
        <TextBlock Grid.Row="0" Grid.Column="1" TextWrapping="Wrap" Name="txtMessage" VerticalAlignment="Center"/>
        <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" BorderThickness="0,1,0,0" BorderBrush="{StaticResource ForegroundBrush}" Padding="4">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,4,0" Name="spButtons"/>
        </Border>
    </Grid>
</Window>
